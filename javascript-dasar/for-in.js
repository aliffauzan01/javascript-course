const array = [1, 'dua', 3, 4, 5, 6, 7, 8, 9, 10];

for (let index in array) {
  console.log(array[index]);
}

const object = {
  nama: 'Budi',
  umur: 30,
};

for (let key in object) {
  console.log(object[key]);
}
